services:
  - type: web
    name: mfa-aligner
    env: python
    plan: free
    buildCommand: |
      apt-get update && apt-get install -y ffmpeg build-essential gfortran libatlas-base-dev
      pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:10000

